import { Layout } from "./Layout";

export const ProfilePage = ({ item }) => {
  return (
    <Layout>
      <div className="archive-page-body">
        <p> Profile</p>
        <p> Name</p>
        <p> Email</p>
      </div>
    </Layout>
  );
};
